# Microbot: tiny LLM
Microbot is a tiny agentic AI tool that can run with Openrouter and OpenAI. It has access to simple commands so it can do everything it needs
without complications. It has no internet access and a restricted python runtime (though not sandboxed). The entire implementation
sits at a few dozen lines of code.
